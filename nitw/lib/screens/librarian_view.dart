import 'package:flutter/material.dart';

class LibrarianView extends StatelessWidget {
  const LibrarianView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}