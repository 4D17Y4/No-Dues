import 'package:flutter/material.dart';

class WardenView extends StatelessWidget {
  const WardenView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
